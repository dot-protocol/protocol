{
  "name": "dot-protocol-workspace",
  "version": "1.0.0",
  "description": "153-byte cryptographic observation format. Ed25519 + SHA-256. Zero dependencies.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:vectors": "pnpm --filter @dotprotocol/core test:vectors",
    "typecheck": "pnpm -r typecheck"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "doi": "10.5281/zenodo.18946074",
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^2.0.0",
    "tsdown": "^0.12.0"
  }
}
